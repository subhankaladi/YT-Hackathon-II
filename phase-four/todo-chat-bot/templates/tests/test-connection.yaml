apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todo-chat-bot.fullname" . }}-test-connection"
  labels:
    {{- include "todo-chat-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todo-chat-bot.fullname" . }}-backend:{{ .Values.backend.service.port }}/docs']
  restartPolicy: Never
